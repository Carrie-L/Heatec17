package com.adsale.HEATEC.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import com.adsale.HEATEC.dao.News;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "NEWS".
*/
public class NewsDao extends AbstractDao<News, String> {

    public static final String TABLENAME = "NEWS";

    /**
     * Properties of entity News.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property NewsID = new Property(0, String.class, "NewsID", true, "NEWS_ID");
        public final static Property IsDelete = new Property(1, Boolean.class, "IsDelete", false, "IS_DELETE");
        public final static Property LType = new Property(2, Integer.class, "LType", false, "LTYPE");
        public final static Property Logo = new Property(3, String.class, "Logo", false, "LOGO");
        public final static Property ShareLink = new Property(4, String.class, "ShareLink", false, "SHARE_LINK");
        public final static Property Title = new Property(5, String.class, "Title", false, "TITLE");
        public final static Property Description = new Property(6, String.class, "Description", false, "DESCRIPTION");
        public final static Property CreateDateTime = new Property(7, String.class, "CreateDateTime", false, "CREATE_DATE_TIME");
        public final static Property UpdateDateTime = new Property(8, String.class, "UpdateDateTime", false, "UPDATE_DATE_TIME");
        public final static Property RecordTimeStamp = new Property(9, String.class, "RecordTimeStamp", false, "RECORD_TIME_STAMP");
        public final static Property PublishDate = new Property(10, String.class, "PublishDate", false, "PUBLISH_DATE");
    };


    public NewsDao(DaoConfig config) {
        super(config);
    }
    
    public NewsDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"NEWS\" (" + //
                "\"NEWS_ID\" TEXT PRIMARY KEY NOT NULL ," + // 0: NewsID
                "\"IS_DELETE\" INTEGER," + // 1: IsDelete
                "\"LTYPE\" INTEGER," + // 2: LType
                "\"LOGO\" TEXT," + // 3: Logo
                "\"SHARE_LINK\" TEXT," + // 4: ShareLink
                "\"TITLE\" TEXT," + // 5: Title
                "\"DESCRIPTION\" TEXT," + // 6: Description
                "\"CREATE_DATE_TIME\" TEXT NOT NULL ," + // 7: CreateDateTime
                "\"UPDATE_DATE_TIME\" TEXT NOT NULL ," + // 8: UpdateDateTime
                "\"RECORD_TIME_STAMP\" TEXT NOT NULL ," + // 9: RecordTimeStamp
                "\"PUBLISH_DATE\" TEXT);"); // 10: PublishDate
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"NEWS\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, News entity) {
        stmt.clearBindings();
 
        String NewsID = entity.getNewsID();
        if (NewsID != null) {
            stmt.bindString(1, NewsID);
        }
 
        Boolean IsDelete = entity.getIsDelete();
        if (IsDelete != null) {
            stmt.bindLong(2, IsDelete ? 1L: 0L);
        }
 
        Integer LType = entity.getLType();
        if (LType != null) {
            stmt.bindLong(3, LType);
        }
 
        String Logo = entity.getLogo();
        if (Logo != null) {
            stmt.bindString(4, Logo);
        }
 
        String ShareLink = entity.getShareLink();
        if (ShareLink != null) {
            stmt.bindString(5, ShareLink);
        }
 
        String Title = entity.getTitle();
        if (Title != null) {
            stmt.bindString(6, Title);
        }
 
        String Description = entity.getDescription();
        if (Description != null) {
            stmt.bindString(7, Description);
        }
        stmt.bindString(8, entity.getCreateDateTime());
        stmt.bindString(9, entity.getUpdateDateTime());
        stmt.bindString(10, entity.getRecordTimeStamp());
 
        String PublishDate = entity.getPublishDate();
        if (PublishDate != null) {
            stmt.bindString(11, PublishDate);
        }
    }

    /** @inheritdoc */
    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public News readEntity(Cursor cursor, int offset) {
        News entity = new News( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // NewsID
            cursor.isNull(offset + 1) ? null : cursor.getShort(offset + 1) != 0, // IsDelete
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // LType
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // Logo
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // ShareLink
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // Title
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // Description
            cursor.getString(offset + 7), // CreateDateTime
            cursor.getString(offset + 8), // UpdateDateTime
            cursor.getString(offset + 9), // RecordTimeStamp
            cursor.isNull(offset + 10) ? null : cursor.getString(offset + 10) // PublishDate
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, News entity, int offset) {
        entity.setNewsID(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setIsDelete(cursor.isNull(offset + 1) ? null : cursor.getShort(offset + 1) != 0);
        entity.setLType(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setLogo(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setShareLink(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setTitle(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setDescription(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
        entity.setCreateDateTime(cursor.getString(offset + 7));
        entity.setUpdateDateTime(cursor.getString(offset + 8));
        entity.setRecordTimeStamp(cursor.getString(offset + 9));
        entity.setPublishDate(cursor.isNull(offset + 10) ? null : cursor.getString(offset + 10));
     }
    
    /** @inheritdoc */
    @Override
    protected String updateKeyAfterInsert(News entity, long rowId) {
        return entity.getNewsID();
    }
    
    /** @inheritdoc */
    @Override
    public String getKey(News entity) {
        if(entity != null) {
            return entity.getNewsID();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
